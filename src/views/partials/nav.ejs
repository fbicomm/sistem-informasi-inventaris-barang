<div class="wrapper">
  <!-- Navbar -->
  <nav class="main-header navbar navbar-expand navbar-white navbar-light">
    <!-- Left navbar links -->
    <ul class="navbar-nav">
      <li class="nav-item">
        <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fa-solid fa-bars"></i></a>
      </li>
    </ul>

    <!-- Right navbar links -->
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <a class="nav-link">
          <div class="custom-control custom-switch">
            <input type="checkbox" class="custom-control-input" id="darkSwitch">
            <label class="custom-control-label" for="darkSwitch">Modo Escuro</label>
          </div>
        </a>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" id="userDropdown" href="#" role="button" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fa-solid fa-user fa-fw"></i>
          <%= locals.user %>
        </a>
        <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
          <li><a class="dropdown-item" href="/account">Editar Conta</a></li>
          <li><div class="dropdown-divider"></div></li>
          <li><a class="dropdown-item" href="/logout">Sair</a></li>
        </ul>
      </li>
    </ul>
  </nav>
  <!-- /.navbar -->

  <!-- Main Sidebar Container -->
  <aside class="main-sidebar sidebar-dark-primary elevation-4">
    <!-- Brand Logo -->
    <a href="/" class="brand-link">
      <img src="/images/logo.png" alt="WGS Logo" class="brand-image img-circle elevation-3" style="opacity: .8">
      <span class="brand-text font-weight-light"><b>Inventario</b>App</span>
    </a>

    <!-- Sidebar -->
    <div class="sidebar">

      <!-- Sidebar Menu -->
      <nav class="mt-2 nav nav-pills nav-sidebar nav-child-indent flex-column">
        <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">
          <!-- Add icons to the links using the .nav-icon class
                with font-awesome or any other icon font library -->
          <li class="nav-item">
            <% if(locals.title == 'Geral') { %>
            <a href="/" class="nav-link active">
            <% } else { %>
            <a href="/" class="nav-link">
            <% } %>
              <i class="nav-icon fa-solid fa-tachometer-alt"></i>
              <p>
                Geral
              </p>
            </a>
          </li>
          <li class="nav-item menu-open">
            <% if(locals.title == 'Items Stock ' || locals.title == 'Entrada de Item' || locals.title == 'Retirada de Item') { %>
            <a href="#" class="nav-link active">
            <% } else { %>
            <a href="#" class="nav-link">
            <% } %>
              <i class="nav-icon fa-solid fa-folder"></i>
              <p>
                Itens
                <i class="right fa-solid fa-angle-left"></i>
              </p>
            </a>
            <ul class="nav nav-treeview">
              <li class="nav-item">
                <% if(locals.title == 'Items Stock ') { %>
                <a href="/items" class="nav-link active">
                <% } else { %>
                <a href="/items" class="nav-link">
                <% } %>
                  <i class="far fa-boxes nav-icon"></i>
                  <p>Estoque</p>
                </a>
              </li>
              <li class="nav-item">
                <% if(locals.title == 'Entrada de Item') { %>
                <a href="/itemsIncoming" class="nav-link active">
                <% } else { %>
                <a href="/itemsIncoming" class="nav-link">
                <% } %>
                  <i class="far fa-box nav-icon"></i>
                  <p>Entrada</p>
                </a>
              </li>
              <li class="nav-item">
                <% if(locals.title == 'Retirada de Item') { %>
                <a href="/itemsWithdrawal" class="nav-link active">
                <% } else { %>
                <a href="/itemsWithdrawal" class="nav-link">
                <% } %>
                  <i class="far fa-box-open nav-icon"></i>
                  <p>Retirada</p>
                </a>
              </li>
            </ul>
          </li>
          <% if(typeof locals.user != 'undefined') { %>
          <li class="nav-item">
            <% if(locals.title == 'Manage Users') { %>
            <a href="/users" class="nav-link active">
            <% } else { %>
            <a href="/users" class="nav-link">
            <% } %>
              <i class="nav-icon fa-solid fa-users"></i>
              <p>
                Gerenciar Usuários
              </p>
            </a>
          </li>
          <% } %>
          <% if(typeof locals.user != 'undefined') { %>
          <li class="nav-item">
            <% if(locals.title == 'Application Logs') { %>
            <a href="/log" class="nav-link active">
            <% } else { %>
            <a href="/log" class="nav-link">
            <% } %>
              <i class="nav-icon fa-solid fa-clipboard-list"></i>
              <p>
                Registros da Aplicação
              </p>
            </a>
          </li>
          <% } %>
        </ul>
      </nav>
      <!-- /.sidebar-menu -->
    </div>
    <!-- /.sidebar -->
  </aside>

  <!-- Content Wrapper. Contains page content -->
  <div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
      <div class="container-fluid">
        <div class="row mb-2">